doctype html
include includes/head.pug
form(action='/signup' method='post')
  input.input(name='username' type='text' placeholder='Username' autocomplete='off')
  br
  input.input(name='email' type='email' placeholder='Email' autocomplete='off')
  br
  button.button(type='submit') sign up
a(href='/') back